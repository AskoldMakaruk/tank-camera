[env]
PORT = "9000"

[config]
skip_core_tasks = true

[tasks.servesignal]
description = "Start Singaling server"
command = "cargo"
args = ["run"]


# ---- BUILD ----

[tasks.build]
description = "Build Signaling Server"
command = "cargo"
args = ["build"]

[tasks.build_release]
description = "Build Signaling Server Release !"
command = "cargo"
args = ["build", "--release"]

